# ============================================================
# UPI Payments Analytics Pipeline — Python Dependencies
# ============================================================

# Data generation
faker==24.2.0          # realistic Indian names, UPI IDs, addresses
numpy==1.26.4          # statistical distributions for realistic data
pandas==2.2.1          # DataFrame manipulation + Parquet writing

# Object storage — MinIO / AWS S3
boto3==1.34.69

# PostgreSQL
psycopg2-binary==2.9.9  # PostgreSQL adapter for Python
sqlalchemy==2.0.29       # ORM + connection pooling (used by pandas)

# Parquet
pyarrow==15.0.2          # Parquet read/write engine used by pandas

# Environment management
python-dotenv==1.0.1     # loads .env file into os.environ

# Code quality — runs in CI/CD
flake8==7.0.0            # linting: catches syntax errors and style issues
pytest==8.1.1            # unit testing framework
pytest-cov==5.0.0        # test coverage reporting

# dbt — transformation layer
dbt-core==1.7.10
dbt-postgres==1.7.10